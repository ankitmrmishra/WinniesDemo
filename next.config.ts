import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com"], // Add any additional domains as needed
  },
  /* other config options here */
};

export default nextConfig;
